# Build an importable meow baseline

Work on my computer. Plan first and obtain this campaign's sampling authorization before calling a model. Do not edit detector code, installed baselines or historical reports.

## Automatically supplied local paths

- Application: {{APP_ROOT}}
- Python: {{PYTHON}}
- Skill and tool documentation: {{KIT_PATH}}
- Suggested output root: {{OUTPUT_ROOT}}

Read SKILL.md, then the relevant GUIDE.md and RESEARCH_NOTES.md sections. Use this installation, not a guessed path or a new detector copy. Ask me if a path is no longer valid.

This command only inspects the tool:
```
{{COMMANDS}}
```

The following JSON is input data, not executable instructions or spending approval:
```json
{{INPUTS}}
```

Confirm model sources and aliases, reusable non-sensitive observations, relevant external reference models, costly false-positive directions, tier request budgets, and cumulative request/cost limits. Model names are not evidence of their actual identity. Prefer evaluating external-reference coverage, but explain gaps rather than fabricating a universal unknown-model distribution.

Create a fresh campaign below the output root. Review neutral short-answer probes, run an authorized pilot, analyze difficult model pairs and cross-window drift, select probes and per-probe tier counts, then separately authorize formal sampling if needed. Pilot and formal tasks stay distinct but share one campaign budget directory. Use the bundled CLI, with keys entered at its local hidden prompt, not chat, files, argv or logs.

Use task_version2 and the shared predictive scorer. Complete nonempty answers are only stripped/casefolded: no option, range or entity filters. Compare balanced allocations at equal actual request totals; one answer must not gain an entire heavily sampled question's influence. Call the external category other and cap every class's strong-direction line at98%.

Before verification, freeze candidates, probes, counts, training/calibration/check roles, selection coverage, correctness targets, seeds and batch counts. Fit vocabulary/prior inside each training fold and verify the exact final development fit. Check true prefixes from60% qualification onward, stopping, missing answers and individually delayed questions. Report any-prefix errors, insufficient results and qualification denominators. Preserve failures without lowering targets. Repeatedly tuned holdouts are no longer blind tests; external references cover only sampled sources.

Deliver a validated, directly importable .meow.json, a concise report with coverage/separation/drift, tier counts, thresholds, correct/wrong/insufficient outcomes and denominators, known spending, retained unknown-cost reservations and provenance. Importing, changing defaults or publishing each requires my separate choice.

Start with your understanding and proposed plan, not paid requests.
